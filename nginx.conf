server {
    listen 80;
    server_name demoadmin.ant24.ai;

    # Important: Serve Certbot challenge files directly on port 80
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        try_files $uri $uri/ =404;
    }

    # Redirect everything else to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# server {
#     listen 443 ssl http2;
#     server_name demoadmin.ant24.ai;

#     ssl_certificate /etc/letsencrypt/live/demoadmin.ant24.ai/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/demoadmin.ant24.ai/privkey.pem;

#     # Optional but recommended for better security
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;

#     location / {
#         proxy_pass http://frontend:3000;  # Changed to match your service name "frontend"
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }